/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: {
        /* -----------------------------------------------------
         * PRIMARY → 메인 영역 (밝은 배경 + 진한 브라운 텍스트)
         * ----------------------------------------------------- */
        "surface-primary": "#F9F6F1", // 메인 배경 (화이트톤 + 브라운)
        "on-surface-primary": "#2E1E12", // 메인 글씨 (아주 진한 브라운)

        /* -----------------------------------------------------
         * SECONDARY → 서브/반전 영역 (완전 브라운 배경 + 밝은 텍스트)
         * ----------------------------------------------------- */
        "surface-secondary": "#4B2E1D", // 서브 배경 (완전 브라운)
        "on-surface-secondary": "#F4ECE4", // 서브 글씨 (화이트톤 + 브라운 약간)

        /* -----------------------------------------------------
         * OPTIONAL – 보더 색 (primary/secondary와 잘 맞는 브라운 톤)
         * ----------------------------------------------------- */
        "border-soft": "#E2D6C8", // 밝은 배경에서 쓰는 은은한 보더
        "border-strong": "#3A2618", // 서브 배경/강조 요소용
      },
      fontFamily: {
        title: ["SUIT", "sans-serif"], // 타이틀용
      },
    },
  },
  plugins: [],
};
